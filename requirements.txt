# Core data & ML
pandas>=2.0.0
numpy>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# PyTorch (Colab provides this — install separately if running locally)
# https://pytorch.org/get-started/locally/
torch>=2.0.0

# Hugging Face stack — versions verified compatible in this submission
transformers==5.6.2
tokenizers==0.22.2
huggingface-hub==1.10.1
safetensors>=0.4.3

# Excel file support
openpyxl>=3.1.0
